"use client"

/**
 * Route dynamique /super-admin-dashboard/<slug>
 * Re-rend le même tableau de bord ; la section active est résolue
 * depuis le slug de l'URL par SuperAdminDashboardClient (usePathname).
 */
export { default } from '../page'
